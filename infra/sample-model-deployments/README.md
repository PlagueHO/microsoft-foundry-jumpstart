# Sample Model Deployments

This folder contains JSON files of sets of models that can be deployed into Microsoft Foundry projects by copying them into the `sample-model-deployments.json` file in the parent folder.
